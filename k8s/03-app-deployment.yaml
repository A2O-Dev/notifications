apiVersion: apps/v1
kind: Deployment
metadata:
  name: gs1-notifications-app
  namespace: gs1
  labels:
    project: gs1
    service: gs1-notifications
    type: app
spec:
  replicas: 2
  selector:
    matchLabels:
      project: gs1
      service: gs1-notifications
      type: app
  template:
    metadata:
      labels:
        project: gs1
        service: gs1-notifications
        type: app
    spec:
      automountServiceAccountToken: false
      containers:
        - name: gs1-notifications-app
          image: a2odev/gs1-notifications:latest
          resources:
            requests:
              memory: '250Mi'
              cpu: '0.25'
            limits:
              memory: '500Mi'
              cpu: '0.5'
          envFrom:
            - secretRef:
                name: gs1-notifications
            - secretRef:
                name: gs1-shared
          ports:
            - containerPort: 3000
