apiVersion: v1
kind: Service
metadata:
  name: notifications-db
  namespace: notifications
spec:
  selector:
    project: notifications
    service: notifications
    type: db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
