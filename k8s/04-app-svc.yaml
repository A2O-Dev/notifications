apiVersion: v1
kind: Service
metadata:
  name: notifications-app
  namespace: notifications
spec:
  selector:
    project: notifications
    service: notifications
    type: app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
